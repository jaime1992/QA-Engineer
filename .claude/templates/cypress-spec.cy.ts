describe('[Modulo] — [Funcionalidad]', () => {
  before(() => {
    // Setup global: datos de prueba, login, etc.
  })

  beforeEach(() => {
    cy.visit('/')
    // Login o estado inicial requerido por cada test
  })

  afterEach(() => {
    // Limpieza opcional
  })

  context('cuando [condicion]', () => {
    it('deberia [comportamiento esperado]', () => {
      // Arrange

      // Act

      // Assert

    })
  })
})
